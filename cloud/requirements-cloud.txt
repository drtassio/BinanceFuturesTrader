# Dependencias do treino remoto (Colab / Kaggle / GPU local).
#
# As versoes dependem do Python da plataforma. O Kaggle roda Python 3.11, para
# o qual numpy 1.26 e scikit-learn 1.3.2 tem binario pronto. O Colab ja esta
# num Python mais novo: forcar essas versoes la fazia o pip baixar o codigo
# fonte do numpy e compila-lo, o que leva muitos minutos e costuma falhar, com
# o scikit-learn 1.3.2 vindo em seguida sem suporte a essa versao.
#
# Em Python antigo mantemos a versao de producao (os scalers sao serializados
# com joblib). Em Python novo usamos a versao disponivel: RobustScaler guarda
# apenas center_ e scale_, que carregam entre versoes com aviso, sem mudar o
# resultado.
numpy<2.0; python_version < "3.12"
numpy; python_version >= "3.12"
pandas>=2.0
pyarrow
scipy
scikit-learn==1.3.2; python_version < "3.12"
scikit-learn>=1.5; python_version >= "3.12"
joblib
python-dotenv
torch
stable-baselines3>=2.3
gymnasium
optuna
statsmodels
hmmlearn
ta
tensorboard
binance-futures-connector
